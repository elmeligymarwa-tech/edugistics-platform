// Same literal value as the local HEADING_FONT constant each existing
// landing component already defines for itself — shared here only for the
// course card, per instruction not to rewrite the other ten components.
export const LANDING_HEADING_FONT = 'font-[family-name:var(--font-league-spartan)] font-bold'
